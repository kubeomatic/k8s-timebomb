apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "kubeomatic-timebomb.fullname" . }}-test-connection"
  labels:
    {{- include "kubeomatic-timebomb.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "kubeomatic-timebomb.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
